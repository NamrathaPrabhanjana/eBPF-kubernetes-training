1. From within any of the containers we have built so far, we can run the sslsniff tool
   - cd /bcc/tools
   - python3 sslsniff.py
   - From another terminal, run, 'curl -XGET https://www.google.com'
2. To check if we are attaching to the right symbols from user-space libs use the nm command
   - nm -D /usr/lib/aarch64-linux-gnu/<libname> | grep <symname>
